# GPU baselines (cuda129) — radtan5 datasets
exp_droidslam:
  Config: config_smoke_rgbdtum.yaml
  NumRuns: 1
  Parameters: {verbose: 0, mode: rgbd}
  Module: droidslam

exp_dpvo:
  Config: config_smoke_rgbdtum.yaml
  NumRuns: 1
  Parameters: {verbose: 0, mode: mono}
  Module: dpvo

exp_mast3rslam:
  Config: config_smoke_rgbdtum.yaml
  NumRuns: 1
  Parameters: {verbose: 0, mode: mono}
  Module: mast3rslam

exp_monogs:
  Config: config_smoke_rgbdtum.yaml
  NumRuns: 1
  Parameters: {verbose: 0, mode: rgbd}
  Module: monogs

# GPU baselines — pinhole datasets
exp_vggtslam:
  Config: config_smoke_eth.yaml
  NumRuns: 1
  Parameters: {verbose: 0, mode: mono}
  Module: vggtslam

exp_vggt:
  Config: config_smoke_eth.yaml
  NumRuns: 1
  Parameters: {verbose: 0, mode: mono}
  Module: vggt

# Non-GPU baselines
exp_orbslam2:
  Config: config_smoke_rgbdtum.yaml
  NumRuns: 1
  Parameters: {verbose: 0, mode: rgbd}
  Module: orbslam2

exp_orbslam3:
  Config: config_smoke_rgbdtum.yaml
  NumRuns: 1
  Parameters: {verbose: 0, mode: rgbd}
  Module: orbslam3

exp_anyfeature:
  Config: config_smoke_rgbdtum.yaml
  NumRuns: 1
  Parameters: {verbose: 0, mode: mono}
  Module: anyfeature

exp_okvis2:
  Config: config_smoke_euroc.yaml
  NumRuns: 1
  Parameters: {verbose: 0, mode: mono-vi}
  Module: okvis2

exp_colmap:
  Config: config_smoke_rgbdtum.yaml
  NumRuns: 1
  Parameters: {verbose: 0, mode: mono}
  Module: colmap
